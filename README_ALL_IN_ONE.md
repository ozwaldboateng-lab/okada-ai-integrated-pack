# Okada AI v2.0 All-in-One

This bundle is intended to answer the question “are all prior files included?” as directly as possible.

It contains:
- the latest cumulative development line based on `v1.17`
- standalone / issue / task prompt support assets from `v1.4`–`v1.7`
- an `archives/releases/` directory with copies of the prior zip releases

Recommended starting points:
- main working tree: `apps/`, `specs/`, `docs/`, `examples/`, `scripts/`
- dual-agent standalone support: `AGENTS.md`, `CLAUDE.md`, `TASKS.md`, `issues/`, `standalone/`
- first prompt handoff: `handoff/task01-paste-ready/`

# Codegen Prompt

Implement production-grade scheduler persistence and champion/challenger promotion guards on top of the existing bootstrap code. Preserve endpoint contracts and extend tests rather than replacing them.

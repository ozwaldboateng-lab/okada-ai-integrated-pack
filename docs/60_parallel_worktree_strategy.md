# Parallel Worktree Strategy

This document is retained only as an optional advanced workflow.

It is **not** the default operating mode for the current handoff pack.
The default mode is two standalone working copies compared by eye.

If you later want coordinated branch/worktree operation, this file can be expanded again.
